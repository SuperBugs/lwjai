# 安全

## 怎么报告

**请不要在公开 issue 里报告安全问题。** 用 GitHub 仓库页面上 **Security → Report a vulnerability**
（私密的安全报告）提交，维护者会在那里回复。

下面这些也请走私密渠道：

- 你在这个仓库（包括历史）里发现了**看起来是真的**个人信息 —— 仓位、账号、联系方式、密钥、本机路径；
- 一种能让发布闸**悄悄失效**的方法（不是"某句话没拦住"，而是"整道闸没跑、却显示通过"）。

## 范围

| 部分 | 说明 |
| --- | --- |
| 公网站点 | 构建产物是纯静态文件，生产环境没有服务端代码。正文里的裸 HTML 经过 `rehype-sanitize` 消毒（白名单在 `src/config/sanitize.ts`）—— XSS 类的问题欢迎报告 |
| 本机后台和工具页 | `/keystatic`、`/_publish`、`/_import`、`/_tidy` 等只在 `astro dev` 里注入，能写盘、能执行 `git commit` / `push`。`/_publish` 有同源检查，但**不是密码学的**：不要用 `--host` 把 dev server 暴露到任何网络上 |
| 发布闸 | 是一张**词表**，不是保证。「没命中已知说法」≠「安全」 |
| 粘贴进来的模型输出 | 视为不可信输入：会被消毒、会过闸，但请照样自己读一遍再发 |

## 闸门漏网不算安全漏洞

"模型真会写出来、却没被拦下"的句子，请直接开 issue（选「发布闸漏网」模板）——
这是改进词表最有用的输入。**例句里的数字请编**，不要贴你自己的真实仓位。
