#!/bin/sh
#
# 发布闸 · 第二道。装法：pnpm hooks:install
#
# 这个目录被 `core.hooksPath` 指着，所以钩子**跟着仓库走**，不是每台机器手动往
# .git/hooks 里塞一份 —— 后者换台机器就没了，而且谁都不知道它曾经存在过。
#
# 【2026-09-21】这里跑两道，顺序是按「错了有多贵」排的：
#   ① 发布闸    —— 仓位 / 成本 / 凭证。发出去就收不回来，所以它永远排第一。
#   ② 稿件体检  —— 这批稿子发出去会不会长歪（空正文、被吃掉的分隔线、后台编不开）。
# 两道都只看**即将被提交的那份字节**（git show :<path>），不是磁盘上那份。
#
# ⚠ `set -e` 是必须的：少了它，第一道拦下之后第二道照跑、而整个钩子按最后一条的
#   退出码算，于是**闸门拦住的东西会被提交进去**，屏幕上还印着一段红字。
#   formatRules.test.ts 的 E 组钉着这三件事（两道都在 + set -e 在）。
set -e
npx tsx scripts/gate/check-staged.ts
exec npx tsx scripts/content/check.ts --staged
