# 参与贡献

先谢谢你。下面几条是这个仓库的规矩，大部分都对应一次真实踩过的坑 ——
细节在 [docs/engineering-notes.md](docs/engineering-notes.md)。

## 这个仓库是怎么维护的

这里是一个**单向镜像**：维护者在自己的私有仓库里开发（那边有真实内容），每次同步时把代码导出到这里。

- **issue 很欢迎**：bug、想法、文档哪里读不懂，都可以直接开。
- **PR 也欢迎，但不会被直接合并。** 维护者会逐行读你的改动，在上游重写一遍，下一次同步带过来。
  所以你会看到改动出现了，但 commit 不是你那一个 —— 这不是不尊重你的工作，是这个结构决定的：
  这边的任何字节都不会被原样搬回有真实内容的那个仓库。
- 同步会整体覆盖这里的文件，所以 PR 被"处理"的标志是它的内容出现在了下一次同步里，而不是被 merge。

## 开发

```bash
pnpm install
pnpm hooks:install   # pre-commit：发布闸 ＋ 稿件体检，只用装一次
pnpm dev             # 站 http://localhost:4321 ，后台 /keystatic
pnpm test            # 全部测试
pnpm build           # 完整的发版链：发布闸 → 稿件体检 → test → astro check → build → pagefind
```

提 PR 之前请至少让 `pnpm build` 在本地跑通（CI 跑的也是它）。

## 必须守的几条

### 1. 任何地方都不许出现真实的仓位

持仓、成本、数量、方向、凭证 —— 代码、测试、文档、issue、PR 描述里都不许有真的。
写测试需要例句时，**数字一律编**，而且编得明显一点。

### 2. 闸门不许能被关掉

`scan()` 没有第二个参数，没有环境变量能跳过它，构建链里发布闸排在 `astro check` 前面。
加开关、调顺序、"只在 CI 里跳过"的 PR 都不会被接受 —— 一个能被关掉的闸门，迟早会在赶时间的那天被关掉。

### 3. 改了闸门的规则表，必须补测试，而且用例要从"模型真的会这么写"来

不许照着正则反推用例：测试自己喂的也是错的写法时，断言和代码会一起错，而且全绿
（[docs/gate.md](docs/gate.md) 第 7 节）。写完"必须拦下"这类用例，**故意把代码改坏一次**，
看它红不红 —— 这是唯一能证明它钉着东西的办法。

### 4. 空状态、失败状态、"我不知道"要长得不一样

写代码时如果发现自己在把两种"没有"合并成一种显示，停下来。
比如「扫了 0 篇」（闸门没跑起来）和「扫了 40 篇没命中」退出码不同；
「模型未标注」（待补）和「人写的、没有模型」（不适用）在页面上不许长得一样。

### 5. 判据只写一处

同一个判断出现在两个地方、只改了一处，是这个仓库最常见的坏法（坑 8）。
要用一个已有的判断，import 它，不要抄一份。

### 6. 样例内容不许编"像真的"的数字

要样例就写清楚它是样例，或者干脆留空。

## PR 自查

- [ ] `pnpm build` 本地全绿
- [ ] 改了行为的地方有测试，而且故意改坏过一次、确认它会红
- [ ] 没有真实的仓位 / 账号 / 联系方式 / 密钥 / 本机路径
- [ ] 没有给闸门加开关，没有调构建链的顺序
- [ ] 新加的"没有"那一档，在页面上和别的档长得不一样

## 用 AI 助手写代码

可以，这个项目本身就是这么写出来的。仓库里没有 `CLAUDE.md` / `AGENTS.md` 这类给工具自动加载的文件 ——
规矩就写在 [docs/engineering-notes.md](docs/engineering-notes.md)，给人看的和给工具看的是同一份，
需要的话自己指给它。
