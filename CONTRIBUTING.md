# 贡献指南

## 维护方式

本仓库为单向镜像：代码在维护者的私有仓库中开发，定期导出至此。

- 欢迎提交 Issue：缺陷、建议、文档问题。
- Pull Request 不会被直接合并。维护者审阅后在上游重新实现，随下次同步发布，最终提交的作者因此与 PR 不同。
- 每次同步会覆盖本仓库的文件。

## 开发

```bash
pnpm install
pnpm hooks:install   # 安装 pre-commit（发布闸与稿件检查）
pnpm dev             # 站点 http://localhost:4321，后台 /keystatic
pnpm test
pnpm build           # 完整发版链，CI 执行同一命令
```

提交 PR 前请确保 `pnpm build` 在本地通过。

## 约定

1. **不得包含真实持仓信息。** 代码、测试、文档、Issue 与 PR 中涉及持仓、成本、数量、方向、凭证的内容一律使用虚构数据。
2. **发布闸不可关闭。** 不接受为 `scan()` 增加开关、跳过检查或调整构建链顺序的改动。
3. **规则变更须附测试，用例取自模型的真实写法。** 不得依据正则反推用例（见 [docs/gate.md](docs/gate.md) 第 7 节）。
   新增"必须拦截"类用例后，须人为破坏一次实现，确认用例会失败。
4. **区分空状态、失败状态与未知状态。** 三者在界面与退出码上必须可区分。
5. **同一判定只实现一处。** 复用既有判定时应导入，不得复制。
6. **示例内容不得包含形似真实的财务数字。**

## PR 检查清单

- [ ] `pnpm build` 本地通过
- [ ] 行为变更附有测试，且已验证测试能够失败
- [ ] 不含真实的持仓、账号、联系方式、密钥或本机路径
- [ ] 未改变发布闸的执行方式与构建链顺序
- [ ] 新增的"无数据"状态与其他状态可区分

## AI 辅助开发

仓库不包含 `CLAUDE.md`、`AGENTS.md` 等由工具自动加载的文件。开发约定见
[docs/engineering-notes.md](docs/engineering-notes.md)，如需可自行提供给所用工具。
